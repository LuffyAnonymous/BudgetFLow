/**
 * src/lib/rate-limiter.ts
 *
 * @deprecated Import from "@/lib/rate-limiter" (the directory index) directly.
 * This file is kept as a re-export for backwards compatibility with existing imports.
 */
export { checkRateLimit, rateLimiter } from "./rate-limiter/index";
export type { RateLimiterProvider, RateLimitResult } from "./rate-limiter/index";
